All benchmarks were run with the following settings:
TIMEOUT: 10 minute
6 times for each regex

Filtered first 13446 regexes. 

The filter is set such that it filteres first regular, than from the correct
regexes with counters and at the end monadic from the rest.

regexes-dtb_14-filtered - passed regular, monadic with counter
regexes-dtb_14-notSupported - non regular or such that we do not handle them
regexes-dtb_14-nonMonadic - non monadic
regexes-dtb_14-noCounters - no counters

694 passed
133 not suppported
10249 no counters
62 non monadic
2308 simple strings

13446 in total.



