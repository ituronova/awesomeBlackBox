All benchmarks were run with the following settings:
TIMEOUT: 10 minute
6 times for each regex

Filtered first 13446 regexes. 

The filter is set such that it filteres first regular, than from the correct
regexes with counters and at the end monadic from the rest.

regexes-dtb_19-filtered - passed regular, monadic with counter
regexes-dtb_19-notSupported - non regular or such that we do not handle them
regexes-dtb_19-nonMonadic - non monadic
regexes-dtb_19-noCounters - no counters

666 passed
157 not suppported
10339 no counters
70 non monadic
2214 simple strings

13446 in total.



