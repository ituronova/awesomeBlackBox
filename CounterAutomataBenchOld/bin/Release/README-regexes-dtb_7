All benchmarks were run with the following settings:
TIMEOUT: 10 minute
6 times for each regex

Filtered first 13446 regexes. 

The filter is set such that it filteres first regular, than from the correct
regexes with counters and at the end monadic from the rest.

regexes-dtb_7-filtered - passed regular, monadic with counter
regexes-dtb_7-notSupported - non regular or such that we do not handle them
regexes-dtb_7-nonMonadic - non monadic
regexes-dtb_7-noCounters - no counters

657 passed
131 not suppported
10446 no counters
58 non monadic
2154 simple strings

13446 in total.



