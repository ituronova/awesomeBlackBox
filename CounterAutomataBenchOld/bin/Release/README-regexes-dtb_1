All benchmarks were run with the following settings:
TIMEOUT: 10 minute
6 times for each regex

Filtered first 13446 regexes. 

The filter is set such that it filteres first regular, than from the correct
regexes with counters and at the end monadic from the rest.

regexes-dtb_1-filtered - passed regular, monadic with counter
regexes-dtb_1-notSupported - non regular or such that we do not handle them
regexes-dtb_1-nonMonadic - non monadic
regexes-dtb_1-noCounters - no counters

732 passed
149 not suppported
10351 no counters
51 non monadic
2163 simple strings

13446 in total.



