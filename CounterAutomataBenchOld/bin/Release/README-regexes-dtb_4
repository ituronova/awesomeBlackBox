All benchmarks were run with the following settings:
TIMEOUT: 10 minute
6 times for each regex

Filtered first 13446 regexes. 

The filter is set such that it filteres first regular, than from the correct
regexes with counters and at the end monadic from the rest.

regexes-dtb_4-filtered - passed regular, monadic with counter
regexes-dtb_4-notSupported - non regular or such that we do not handle them
regexes-dtb_4-nonMonadic - non monadic
regexes-dtb_4-noCounters - no counters

680 passed
137 not suppported
10369 no counters
64 non monadic
2196 simple strings

13446 in total.



