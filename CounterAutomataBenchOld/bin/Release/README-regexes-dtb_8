All benchmarks were run with the following settings:
TIMEOUT: 10 minute
6 times for each regex

Filtered first 13446 regexes. 

The filter is set such that it filteres first regular, than from the correct
regexes with counters and at the end monadic from the rest.

regexes-dtb_8-filtered - passed regular, monadic with counter
regexes-dtb_8-notSupported - non regular or such that we do not handle them
regexes-dtb_8-nonMonadic - non monadic
regexes-dtb_8-noCounters - no counters

685 passed
167 not suppported
10249 no counters
60 non monadic
2285 simple strings

13446 in total.



