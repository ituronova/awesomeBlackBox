All benchmarks were run with the following settings:
TIMEOUT: 10 minute
6 times for each regex

Filtered first 13446 regexes. 

The filter is set such that it filteres first regular, than from the correct
regexes with counters and at the end monadic from the rest.

regexes-dtb_5-filtered - passed regular, monadic with counter
regexes-dtb_5-notSupported - non regular or such that we do not handle them
regexes-dtb_5-nonMonadic - non monadic
regexes-dtb_5-noCounters - no counters

655 passed
140 not suppported
10351 no counters
74 non monadic
2226 simple strings

13446 in total.



