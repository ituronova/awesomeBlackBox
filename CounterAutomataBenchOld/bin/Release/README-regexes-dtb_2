All benchmarks were run with the following settings:
TIMEOUT: 10 minute
6 times for each regex

Filtered first 13446 regexes. 

The filter is set such that it filteres first regular, than from the correct
regexes with counters and at the end monadic from the rest.

regexes-dtb_2-filtered - passed regular, monadic with counter
regexes-dtb_2-notSupported - non regular or such that we do not handle them
regexes-dtb_2-nonMonadic - non monadic
regexes-dtb_2-noCounters - no counters

706 passed
141 not suppported
10323 no counters
64 non monadic
2212 simple strings

13446 in total.



