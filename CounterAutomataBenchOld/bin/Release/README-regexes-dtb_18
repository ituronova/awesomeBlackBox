All benchmarks were run with the following settings:
TIMEOUT: 10 minute
6 times for each regex

Filtered first 13446 regexes. 

The filter is set such that it filteres first regular, than from the correct
regexes with counters and at the end monadic from the rest.

regexes-dtb_18-filtered - passed regular, monadic with counter
regexes-dtb_18-notSupported - non regular or such that we do not handle them
regexes-dtb_18-nonMonadic - non monadic
regexes-dtb_18-noCounters - no counters

700 passed
146 not suppported
10313 no counters
62 non monadic
2225 simple strings

13446 in total.



