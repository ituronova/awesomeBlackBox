All benchmarks were run with the following settings:
TIMEOUT: 10 minute
6 times for each regex

Filtered first 13446 regexes. 

The filter is set such that it filteres first regular, than from the correct
regexes with counters and at the end monadic from the rest.

regexes-dtb_10-filtered - passed regular, monadic with counter
regexes-dtb_10-notSupported - non regular or such that we do not handle them
regexes-dtb_10-nonMonadic - non monadic
regexes-dtb_10-noCounters - no counters

716 passed
150 not suppported
10253 no counters
68 non monadic
2259 simple strings

13446 in total.



